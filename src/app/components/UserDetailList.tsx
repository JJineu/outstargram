"use client";

export default function UserDetailList() {
  return (
    <>
      <div>{/*bar - POSTS SAVED LIKED 따로 빼야된다.*/}</div>
{/* 
      <ModalPortal>
        <PostModal onClose={() => setOpenModal(false)}>
          <PostDetail post={post} />
        </PostModal>
      </ModalPortal> */}
    </>
  );
}
